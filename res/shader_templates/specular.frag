    vec3 spec_pos_{{ node_id }} = vec3({{ pos.x }}, {{ pos.y }}, {{ pos.z }});
    vec3 spec_color_{{ node_id }} = vec3({{ color.r }}, {{ color.g }}, {{ color.b }});
    vec3 spec_viewDir_{{ node_id }} = normalize(viewPos - worldPos.xyz);
    float spec_shininess_{{ node_id }} = 128;
    float spec_specularStrength_{{ node_id }} = 0.3;
    vec3 spec_lightDir_{{ node_id }} = normalize(spec_pos_{{ node_id }} - worldPos.xyz);
    vec3 spec_halfwayDir_{{ node_id }} = normalize(spec_lightDir_{{ node_id }} + spec_viewDir_{{ node_id }});
    float spec_{{ node_id }} = pow(max(dot(g_norm, spec_halfwayDir_{{ node_id }}), 0.0), spec_shininess_{{ node_id }});
    vec3 pointSpecular_{{ node_id }} = spec_color_{{ node_id }} * spec_{{ node_id }} * spec_specularStrength_{{ node_id }};
    FragColor.xyz = FragColor.xyz + pointSpecular_{{ node_id }};